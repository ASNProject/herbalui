import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function HerbalEdu() {
    return (
        <SafeAreaView>
            <View>
                <Text>Halaman herbal edu</Text>
            </View>
        </SafeAreaView>
    )
}