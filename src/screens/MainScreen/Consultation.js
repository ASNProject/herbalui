import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Consultation() {
    return (
        <SafeAreaView>
            <View>
                <Text>Halaman konsultasi</Text>
            </View>
        </SafeAreaView>
    )
}