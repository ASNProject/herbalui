import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function HalalCenter() {
    return (
        <SafeAreaView>
            <View>
                <Text>Halaman halal center</Text>
            </View>
        </SafeAreaView>
    )
}